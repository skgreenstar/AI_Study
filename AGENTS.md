# AGENTS.md — AI 시각 백과사전 공통 에이전트 규칙

이 파일은 이 저장소에서 작업하는 모든 AI 에이전트와 기여자가 따라야 할 공통 규칙이다.
목표는 여러 사람이 다른 시점에 문서를 만들어도 하나의 연결된 시각 백과사전처럼 보이게 하는 것이다.

## 1. 프로젝트 목표

- AI 개념을 완전 입문자도 이해할 수 있도록 시각적으로 설명한다.
- 각 개념을 독립된 글로 끝내지 않고 선수지식, 관련 개념, 다음 학습으로 연결한다.
- 생활 비유로 직관을 만들고 정확한 전문용어와 최소 수식으로 보완한다.
- 학습자가 마지막에 개념을 자기 말로 설명할 수 있게 만든다.

## 2. 기준 디자인 레퍼런스

새 항목의 기본 완성도와 시각 스타일은 `topics/diffusion-prerequisites.html`을 기준으로 한다.
이 문서는 최초 파일명 `diffusion_prerequisites_visual_guide.html`로 만든 레퍼런스의 저장소 버전이다.

새 문서를 만들기 전에 반드시 이 파일을 읽고 다음 요소를 재사용한다.

- 보라색·분홍색 중심의 디자인 토큰과 의미 색상
- 넓은 히어로가 아니라 학습 목표가 즉시 보이는 상단 영역
- 단계별 이동 목차
- 흰색 카드와 충분한 여백
- 핵심 흐름을 보여주는 node, arrow, flow 패턴
- 필수·병행·나중에를 구분하는 상태 색상
- 수식 본문과 기호 해설을 분리하는 equation, decode 패턴
- 실무 연결을 설명하는 카드
- 직접 눌러보는 체크리스트와 details/summary 퀴즈
- 모바일에서 2열이 1열로 바뀌는 반응형 구조

레퍼런스를 그대로 복제하여 제목만 바꾸지 않는다. 주제의 관계에 맞는 도식으로 교체하되
정보 밀도, 설명 난이도, 카드 품질과 시각적 위계는 레퍼런스 이상으로 유지한다.

### 디자인 토큰

~~~css
--bg: #f4f1ff;
--ink: #202038;
--muted: #69677c;
--card: #ffffff;
--violet: #7357e8;
--pink: #ef6797;
--blue: #4d8df7;
--mint: #41bda5;
--yellow: #ffc94b;
--red: #ef6a6a;
--line: #e4def7;
~~~

색상 의미:

- 분홍색: 먼저 알아야 하는 핵심 또는 필수
- 노란색: 학습 중 함께 보충
- 민트색: 후속 확장 또는 이해 완료
- 빨간색: 오해, 주의, 피해야 할 접근
- 보라색: 주요 흐름, 핵심 개념, 탐색

## 3. 절대 규칙

1. 사용자가 명시적으로 요청하지 않는 한 기존 내용은 삭제하거나 축약하지 않는다.
2. 기존 디자인 토큰, 색상 의미, 카드 구조와 문서 순서를 유지한다.
3. 글만 길게 나열하지 않는다. 관계가 있는 내용은 흐름도, 단계도, 비교표 또는 개념 지도로 표현한다.
4. 장식만을 위한 이미지나 도형은 만들지 않는다. 모든 시각 요소에는 학습 목적이 있어야 한다.
5. 수식은 결과만 제시하지 않는다. 모든 주요 기호에 발음, 뜻, 역할과 문장으로 읽는 법을 붙인다.
6. 비유는 이해를 돕는 범위와 실제 개념과 달라지는 한계를 함께 설명한다.
7. 선수지식, 관련 항목, 다음 학습 링크를 반드시 작성한다.
8. 새 항목을 추가하면 연결되는 기존 항목에도 역방향 링크를 추가한다.
9. 최신성이나 정확성 확인이 필요한 내용은 공식 문서, 원 논문 등 1차 출처를 우선한다.
10. 출처 URL과 확인일을 문서 하단에 기록한다.
11. 모바일과 데스크톱에서 모두 읽을 수 있어야 한다.
12. main 브랜치에 직접 작업하지 않고 별도 브랜치와 Pull Request를 사용한다.
13. 본문의 전문용어는 별도 용어사전으로 분리하지 않는다. 용어가 실제 등장하는 위치 옆에 작은 `i` 아이콘을 두고, 클릭하면 그 자리에서 설명이 펼쳐지게 한다.
14. 용어 설명에는 영문 표기, IPA 실제 영어 발음, 한글 발음, 한글 뜻, 쉬운 개념 설명, 직관적 비유와 3-5단계 미니 흐름도를 포함한다.
15. 공통 자산 `assets/inline-terms.css`와 `assets/inline-terms.js`를 모든 학습 HTML에 연결하고, 새 용어가 필요하면 공통 정의를 확장한다.
16. 용어 아이콘은 같은 용어에 페이지당 한 번만 사용하고, 일반 문장 중간이나 단순 반복 위치에는 붙이지 않는다.
17. 용어 아이콘 개수에는 제한을 두지 않는다. 실제 이해에 필요한 전문용어는 빠짐없이 다루되, 이미 본문에서 충분히 설명한 용어에는 중복 설명을 만들지 않는다.
18. 아이콘은 제목·개념 카드·흐름도 노드처럼 용어가 명확한 위치에만 배치하고 문맥과 무관한 자동 삽입을 금지한다.

## 4. 작업 입력 템플릿

사용자가 아래 항목 중 일부만 제공하면 나머지는 합리적으로 자동 판단한다.
주제만 제공된 경우에도 불필요한 질문으로 작업을 멈추지 않는다.

~~~text
주제: [새로 공부할 개념]
대상: [완전 입문자 / 대학생 / 현업 개발자]
목표: [이 자료를 보고 무엇을 이해해야 하는지]
난이도: [입문 / 기초 / 중급 / 심화]
선수지식: [먼저 알아야 할 개념 또는 자동 판단]
기존 연결 항목: [현재 백과사전의 관련 개념]
특별히 강조할 내용: [그림과 직관 / 수식 해부 / 코드 / 실무 사례]
~~~

기본값:

- 대상: 완전 입문자
- 난이도: 개념을 분석하여 자동 판단
- 선수지식: 저장소의 기존 항목을 조사하여 자동 연결
- 강조 방식: 그림과 직관을 우선하고 수식 해부와 실무 사례를 함께 제공
- 언어: 쉬운 한국어, 필요한 경우 영문 전문용어 병기

## 5. 새 항목 생성 절차

### 5.1 저장소 조사

1. README.md에서 현재 항목 목록을 확인한다.
2. topics 디렉터리에서 중복되거나 관련된 문서를 찾는다.
3. 새 주제의 선수지식, 상위 개념, 하위 개념과 유사 개념을 결정한다.
4. 기존 문서에 연결해야 할 역방향 링크를 정한다.

### 5.2 파일 생성

1. topics/_template.html을 복사한다.
2. 파일명은 영문 kebab-case를 사용한다.
3. 예: conditional-probability.html, gradient-descent.html
4. 문서 ID는 확장자를 제외한 파일명과 동일하게 한다.
5. index.html의 학습 항목 목록에 새 카드를 추가한다.
6. README.md의 현재 항목에도 새 문서를 추가한다.

### 5.3 연결 갱신

새 문서에만 링크를 넣고 끝내지 않는다.

~~~text
기존 선수지식 문서 ──다음 학습──> 새 문서
기존 관련 문서     <──관련 항목──> 새 문서
새 문서           ──다음 학습──> 후속 문서
~~~

후속 문서가 아직 없다면 비활성 카드 또는 예정 항목으로 표시하되 존재하지 않는 링크를 만들지 않는다.

## 6. 모든 문서의 고정 구조

다음 순서를 유지한다.

1. 문서 메타데이터
2. 한눈에 보는 큰 그림
3. 생활 비유
4. 핵심 개념 카드
5. 최소 수식 해부
6. 실제 AI 활용 사례
7. 자주 하는 오해
8. 3문항 확인 퀴즈와 해설
9. 선수지식·관련 항목·다음 학습
10. 출처·작성자·검토자·수정일

내용이 많아져도 필요한 설명을 임의로 제거하지 않는다. 섹션 내부에서 카드를 추가하거나 접기 요소를 사용한다.

## 7. 시각화 선택 규칙

| 설명하려는 관계 | 우선 표현 |
| --- | --- |
| 시간이나 처리 순서 | 가로 또는 세로 흐름도 |
| 상위·하위 개념 | 트리 또는 계층 지도 |
| 두 방법의 차이 | 비교표 또는 나란한 카드 |
| 분포·변화·성능 | 축과 범례가 있는 그래프 |
| 수식 구성요소 | 색으로 대응되는 기호 해부 카드 |
| 학습 단계 | 진행 경로 또는 체크리스트 |
| 시스템 구성 | 컴포넌트·데이터 흐름도 |

한 화면에 너무 많은 화살표를 넣지 않는다. 흐름이 복잡하면 여러 단계로 나눈다.
색상만으로 의미를 구분하지 않고 텍스트 라벨을 함께 사용한다.

## 8. 수식 작성 규칙

모든 핵심 수식은 아래 형식을 따른다.

~~~text
수식:
x_t = sqrt(alpha_bar_t) x_0 + sqrt(1-alpha_bar_t) epsilon

기호:
- x_t: 엑스 티 — t시점의 데이터
- alpha_bar_t: 알파 바 티 — 원본이 남아 있는 비율
- epsilon: 엡실론 — 정규분포에서 뽑은 노이즈

문장으로 읽기:
t시점 데이터는 남아 있는 원본과 추가된 노이즈의 합이다.

왜 필요한가:
원본에서 임의의 노이즈 단계 x_t를 한 번에 만들 수 있다.
~~~

수식 유도를 생략하는 경우 무엇을 생략했는지 밝힌다.
완전 입문자 문서에서는 증명보다 기호와 흐름의 의미를 우선한다.

## 9. 실무 사례 작성 규칙

실무 사례는 제품 이름만 나열하지 않는다.

각 사례에 다음을 포함한다.

- 해결하려는 문제
- 입력 데이터
- 모델이 배우는 것
- 출력 결과
- 이 문서의 개념이 사용되는 지점
- 적용할 때의 한계 또는 주의점

## 10. 퀴즈 작성 규칙

- 정확히 3문항을 기본으로 한다.
- 단순 용어 암기보다 개념 흐름을 확인한다.
- 정답과 한두 문장의 해설을 함께 제공한다.
- HTML details/summary를 사용해 답을 펼쳐볼 수 있게 한다.
- 질문만 읽었을 때 정답이 노출되지 않게 한다.

## 11. 문서 메타데이터

각 문서 상단 또는 하단에 다음 정보를 기록한다.

~~~yaml
id: document-id
title: 문서 제목
area: ML | 확률통계 | 생성모델 | LLM | MLOps | 인프라 | 기타
level: 입문 | 기초 | 중급 | 심화
prerequisites:
  - prerequisite-id
next:
  - next-document-id
related:
  - related-document-id
author: 작성자
reviewer: 검토자
status: draft | review | reviewed
updated: YYYY-MM-DD
~~~

## 12. 공통 실행 프롬프트

아래 프롬프트를 그대로 복사하고 대괄호만 바꾸어 사용한다.

~~~text
이 저장소의 AGENTS.md와 CONTRIBUTING.md를 먼저 읽고 모든 규칙을 적용해줘.

AI 시각 백과사전에 다음 항목을 추가해줘.

주제: [주제]
대상: [대상 또는 자동 판단]
목표: [학습 목표 또는 자동 판단]
난이도: [난이도 또는 자동 판단]
선수지식: [선수지식 또는 자동 판단]
기존 연결 항목: [관련 항목 또는 자동 판단]
특별히 강조할 내용: [그림과 직관 / 수식 해부 / 코드 / 실무 사례]

작업 요구사항:
- topics/diffusion-prerequisites.html을 디자인·완성도 기준으로 먼저 읽고, topics/_template.html을 기반으로 새 문서를 작성해.
- 기존 디자인과 문서 구조를 유지해.
- 기존 내용은 삭제하거나 축약하지 마.
- 글보다 학습 목적의 흐름도, 비교표, 그래프와 시각적 비유를 우선해.
- 수식의 모든 주요 기호에 한국어 발음, 뜻, 역할과 문장으로 읽기를 제공해.
- 실제 AI 활용 사례를 입력, 학습 대상, 출력과 한계까지 연결해.
- 선수지식, 다음 학습, 관련 문서를 양방향으로 연결해.
- index.html과 README.md의 항목 목록을 갱신해.
- 출처와 확인일을 기록해.
- 완료 후 내부 링크, 모바일 레이아웃, 용어, 색상 범례와 문서 중복을 검사해.
- 별도 브랜치에 커밋하고 Pull Request용 변경 요약과 검토 체크리스트를 작성해.
~~~

## 13. Pull Request 결과 형식

작업을 마친 에이전트는 다음을 보고한다.

~~~markdown
## 추가한 항목

- 문서:
- 분야:
- 난이도:
- 학습 목표:

## 연결 변경

- 선수지식:
- 다음 학습:
- 관련 항목:
- 역방향 링크를 수정한 기존 문서:

## 시각 요소

- 추가한 흐름도:
- 추가한 비교표 또는 그래프:
- 수식 해부:

## 검증

- [ ] 내부 링크 확인
- [ ] 모바일 레이아웃 확인
- [ ] 기존 내용 보존
- [ ] 출처와 확인일 확인
- [ ] 퀴즈와 해설 확인
~~~

## 14. 완료 조건

다음 조건을 모두 만족해야 작업 완료로 판단한다.

- 새 항목이 index.html과 README.md에서 발견된다.
- 새 항목과 기존 항목 사이에 실제 링크가 연결된다.
- 빈 플레이스홀더가 남아 있지 않다.
- 핵심 개념에 시각적 설명이 존재한다.
- 주요 수식에 기호 해설이 존재한다.
- 실무 사례와 한계가 존재한다.
- 퀴즈 3문항과 해설이 존재한다.
- 출처와 확인일이 존재한다.
- Pull Request 설명에 변경 범위와 검증 결과가 기록된다.
