const categoryId=document.body.dataset.category;
const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));
function renderTopic(t){return `<a class="topic" href="../${esc(t.href)}"><span class="tag">${esc(t.tag||'학습 항목')}</span><h2>${esc(t.title)}</h2><p>${esc(t.description||'')}</p>${t.path?`<div class="path">${esc(t.path)}</div>`:''}<span class="status">${esc(t.status||'학습 시작')} →</span></a>`}
function render(data){const category=(data.categories||[]).find(c=>c.id===categoryId);if(!category)throw Error('unknown category');document.documentElement.style.setProperty('--accent',category.color||'#7357e8');document.title=category.title+' | AI Study';document.getElementById('icon').textContent=category.icon||'📚';document.getElementById('title').textContent=category.title;document.getElementById('description').textContent=category.pageDescription||category.description||'';const root=document.getElementById('topics');if(category.topics?.length){root.innerHTML='<div class="grid">'+category.topics.map(renderTopic).join('')+'</div>'}else{root.innerHTML='<div class="empty"><div class="plus">＋</div><h2>첫 번째 지식을 기다리고 있어요</h2><p>공통 프롬프트를 사용해 이 카테고리의 첫 문서를 추가할 수 있습니다.</p><a href="../prompts/agent-template.md">에이전트 템플릿 열기 →</a></div>'}}
fetch('../data/knowledge-categories.json').then(r=>{if(!r.ok)throw Error(r.status);return r.json()}).then(render).catch(()=>{document.getElementById('topics').innerHTML='<div class="error">카테고리 데이터를 불러오지 못했습니다. data/knowledge-categories.json을 확인해 주세요.</div>'});